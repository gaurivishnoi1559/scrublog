[build-system]
requires = ["setuptools>=84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "scrublog"
version = "0.3.1"
description = "Opinionated log housekeeping tool with dry-run support"
requires-python = ">=3.9"

[project.scripts]
scrublog = "logwash.cli:main"
